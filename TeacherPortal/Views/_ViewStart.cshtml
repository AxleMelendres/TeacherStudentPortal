@{
    var ctrl = ViewContext.RouteData.Values["controller"]?.ToString() ?? "";
    if (ctrl == "Account") { Layout = null; }
    else if (ctrl == "StudentDashboard") { Layout = "_StudentLayout"; }
    else { Layout = "_TeacherLayout"; }
}
